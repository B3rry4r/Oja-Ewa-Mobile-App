# Fastfile
opt_out_usage

platform :ios do
  desc "Build and upload to TestFlight (Fastlane-managed signing)"
  lane :beta do
    setup_ci

    # -----------------------------
    # Keychain for CI
    # -----------------------------
    create_keychain(
      name: "signing_keychain",
      password: ENV["MATCH_KEYCHAIN_PASSWORD"],
      default_keychain: true,
      unlock: true,
      timeout: 3600,
      lock_when_sleeps: false
    )

    # -----------------------------
    # App Store Connect API Key
    # -----------------------------
    api_key = app_store_connect_api_key(
      key_id: ENV["APP_STORE_CONNECT_API_KEY_ID"],
      issuer_id: ENV["APP_STORE_CONNECT_ISSUER_ID"],
      key_filepath: "./AuthKey.p8"
    )

    # -----------------------------
    # MATCH — THIS HANDLES ALL SIGNING
    # -----------------------------
    match(
      type: "appstore",
      readonly: true,
      app_identifier: "com.wawuafrica.ojaewa",
      git_url: ENV["MATCH_GIT_URL"],
      git_basic_authorization: Base64.strict_encode64(
        "#{ENV['MATCH_GIT_TOKEN']}:x-oauth-basic"
      ),
      keychain_name: "signing_keychain",
      keychain_password: ENV["MATCH_KEYCHAIN_PASSWORD"],
      api_key: api_key,
      clone_branch_directly: true
    )

    # -----------------------------
    # Get profile and team info from the installed profile
    # -----------------------------
    profile_path = ENV["sigh_com.wawuafrica.ojaewa_appstore_profile-path"]
    UI.message("Profile path: #{profile_path}")
    
    # Extract team ID and UUID from the profile
    require 'plist'
    profile_plist = Plist.parse_xml(`security cms -D -i "#{profile_path}"`)
    profile_uuid = profile_plist["UUID"]
    team_id = profile_plist["TeamIdentifier"].first
    team_name = profile_plist["TeamName"]
    
    UI.message("=" * 50)
    UI.message("Profile UUID: #{profile_uuid}")
    UI.message("Profile Name: #{profile_plist['Name']}")
    UI.message("Team ID from profile: #{team_id}")
    UI.message("Team Name from profile: #{team_name}")
    UI.message("Team ID from ENV: #{ENV['APPLE_TEAM_ID']}")
    UI.message("=" * 50)
    
    # Use the team ID from the profile instead of ENV
    actual_team_id = team_id

    # -----------------------------
    # SET TEAM ID AND PROFILE IN PROJECT
    # -----------------------------
    update_code_signing_settings(
      use_automatic_signing: false,
      path: "Runner.xcodeproj",
      team_id: actual_team_id,
      bundle_identifier: "com.wawuafrica.ojaewa",
      profile_uuid: profile_uuid,
      code_sign_identity: "iPhone Distribution",
      build_configurations: ["Release"]
    )

    # -----------------------------
    # Build number bump - TIMESTAMP BASED
    # -----------------------------
    UI.message("=== BUILD NUMBER INFO ===")
    
    # Generate timestamp-based build number (last 9 digits of Unix timestamp)
    # This guarantees uniqueness and always increments
    # Example: 1736428800 → 736428800
    new_build_number = Time.now.to_i.to_s[-9..-1].to_i
    
    UI.success("✅ Using timestamp-based build number: #{new_build_number}")
    UI.message("This ensures the build number is always unique and higher than previous builds")
    UI.message("=" * 50)
    
    # Update the build number in Xcode project
    increment_build_number(
      build_number: new_build_number,
      xcodeproj: "Runner.xcodeproj"
    )
    
    # CRITICAL: Update pubspec.yaml build number BEFORE flutter build
    # Flutter build reads from pubspec.yaml and will override Xcode changes
    # Navigate to project root (Fastfile is in ios/fastlane, need to go up 2 levels)
    project_root = File.expand_path("../..", Dir.pwd)
    pubspec_path = File.join(project_root, "pubspec.yaml")
    
    UI.message("Project root: #{project_root}")
    UI.message("Pubspec path: #{pubspec_path}")
    
    if File.exist?(pubspec_path)
      pubspec_content = File.read(pubspec_path)
      
      # Replace the version line (e.g., version: 1.0.0+33 → version: 1.0.0+34)
      updated_content = pubspec_content.gsub(/^version:\s*(\d+\.\d+\.\d+)\+\d+/, "version: \\1+#{new_build_number}")
      File.write(pubspec_path, updated_content)
      
      UI.success("✅ Updated pubspec.yaml build number to: #{new_build_number}")
      
      # Verify the change
      version_line = File.read(pubspec_path).match(/^version:\s*(.+)$/)
      UI.message("Current pubspec.yaml version: #{version_line[1]}")
    else
      UI.error("❌ pubspec.yaml not found at: #{pubspec_path}")
      UI.important("Continuing with Xcode build number only...")
    end
    
    # Store for reference
    ENV["BUILD_NUMBER"] = new_build_number.to_s

    # -----------------------------
    # Flutter build (no codesign)
    # -----------------------------
    Dir.chdir("..") do
      sh("flutter", "build", "ios", "--release", "--no-codesign", "--build-number=#{new_build_number}")
    end    

    # -----------------------------
    # Xcode archive + export
    # -----------------------------
    build_app(
      workspace: "Runner.xcworkspace",
      scheme: "Runner",
      export_method: "app-store",
      export_options: {
        teamID: actual_team_id,
        provisioningProfiles: {
          "com.wawuafrica.ojaewa" => profile_uuid
        }
      },
      clean: true
    )

    # -----------------------------
    # Upload to TestFlight
    # -----------------------------
    upload_to_testflight(
      api_key: api_key,
      skip_waiting_for_build_processing: false
    )

  ensure
    delete_keychain(
      name: "signing_keychain"
    ) if File.exist?(File.expand_path("~/Library/Keychains/signing_keychain-db"))
  end
end
