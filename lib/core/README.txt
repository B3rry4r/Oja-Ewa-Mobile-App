Core shared code (cross-feature):
- errors/exceptions
- network (dio)
- storage (secure storage / hive / drift)
- utils/extensions
- widgets/design system primitives

(placeholder file so the folder is committed even before code exists)
